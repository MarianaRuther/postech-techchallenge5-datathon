# ============================================
# requirements.txt — App Streamlit Passos Mágicos
# Pinado pra deploy no Streamlit Community Cloud
# Stack ENXUTA: só o que o app precisa rodar.
# Não inclui torch, shap, sentence-transformers, etc — esses ficam só no repo de notebooks.
# ============================================

streamlit>=1.32,<1.45
pandas>=2.1,<2.3
numpy>=1.26,<2.1

# Mesma versão do scikit-learn usada no treino (notebook 04) — IMPORTANTE pra desserializar o .pkl
scikit-learn>=1.4,<1.6

joblib>=1.3
pyarrow>=15.0
matplotlib>=3.8
